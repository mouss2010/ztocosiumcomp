import * as S from './todoTitle.css';

export const TitleComponent: angular.IComponentOptions = {
    template: require('./todoTitle.html'),
    controller: class implements angular.IController {
        constructor() { }
    }
};
